
export default ["$location", "ProductSvc", function ($location, productSvc) {

    // this.products = await productSvc.loadProducts();
    // console.log(this.products);

    // $location.url("/login")

}]